[deploy]
startCommand = "cd otc_pulse && python manage.py migrate --no-input && python manage.py collectstatic --no-input && gunicorn otc_pulse.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --access-logfile -"
healthcheckPath = "/health/"
restartPolicyType = "on_failure"
