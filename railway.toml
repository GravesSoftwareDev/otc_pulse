[deploy]
startCommand = "cd otc_pulse && python manage.py migrate --no-input --fake-initial && python manage.py collectstatic --no-input && gunicorn otc_pulse.wsgi --bind 0.0.0.0:$PORT"
healthcheckPath = "/"
restartPolicyType = "on_failure"
